steps:
  - name: "gcr.io/google.com/cloudsdktool/cloud-sdk"
    entrypoint: "gcloud"
    args:
      - "app"
      - "deploy"
      - "app.yaml"
      - "cron.yaml"
      - "--quiet"
options:
  logging: CLOUD_LOGGING_ONLY

timeout: "900s"
